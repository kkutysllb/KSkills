实际 SKILL.md 数量是 96，而 README 写的也是 96，数量正确。没有其他问题需要修。

---

## 计划：KSkills 工具链与 CI 增强

### 目标
为 KSkills 仓库的 skill-packaging CI pipeline 添加冒烟测试 job，确保完整的工具链（打包→发布→安装）端到端可用。

### 实现步骤

#### 1. 添加 smoke-test job 到 validate.yml
- **文件**: `.github/workflows/validate.yml`
- **操作**: 在 `validate` job 之后新增 `smoke-test` job
- **内容**: 
  - `needs: validate`
  - runs-on: ubuntu-latest
  - 检出仓库
  - 设置 Python 3.10
  - 安装依赖（`pip install pyyaml`）
  - 运行 `scripts/test_toolchain.sh coding/refactor` 进行冒烟测试
- **状态**: ✅ 已完成 — `validate.yml` 已编辑，添加了完整的 `smoke-test` job

#### 2. 将 test_toolchain.sh 加入 Git 追踪
- **文件**: `scripts/test_toolchain.sh`
- **操作**: 确保文件已加入 Git 追踪且具有可执行权限
- **状态**: ✅ 已完成 — `git add --chmod=+x scripts/test_toolchain.sh`

#### 3. 本地验证冒烟测试
- **操作**: 运行 `./scripts/test_toolchain.sh coding/refactor` 确认 6 个步骤全部通过
- **状态**: ✅ 已完成 — 测试通过，输出显示 6/6 步骤通过

#### 4. README 技能数量检查
- **操作**: 验证技能数量计数是否准确
- **状态**: ✅ 已完成 — 实际 96 个 SKILL.md，README 显示 96 个，一致，无需修改

### 测试策略
- 本地已运行 `test_toolchain.sh coding/refactor` 全部 6 步骤通过 ✅
- CI 中 `smoke-test` job 会在 PR merge 到 main 时自动执行

### 风险与注意事项
- 低风险 — 仅新增 CI job，不影响现有逻辑
- `test_toolchain.sh` 依赖 `coding/refactor` fixture 技能，确保该技能目录存在且结构完整
- 脚本使用临时工作区，不污染仓库状态

---

当前计划已按 todo 列表的五个步骤完成。需要我推送变更或做其他进一步操作吗？