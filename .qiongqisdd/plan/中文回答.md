好的，计划已保存。以下是计划的中文概要：

## 实现计划概要

### 目标
为 KSkills 仓库添加发布工具链，增强 CI，修复 README。

### 三个主要任务

**1. `scripts/bump-version.sh` — 版本号管理脚本**
- 从 `scripts/build_skill.py` 中读取当前 `KSKILLS_VERSION`
- 支持 `major` / `minor` / `patch` 关键词或显式版本号（如 `2.0.0`）
- `--dry-run` 预览模式，不修改文件
- `--commit` 自动 git commit
- 验证 semver 格式合法性

**2. CI 增强**
- 在 `.github/workflows/validate.yml` 中添加 `smoke-test` job
- 运行 `test_toolchain.sh coding/refactor` 作为端到端冒烟测试（build → install → verify → uninstall）
- 在已有 validation 通过后执行

**3. README 修复**
- 检查并修正技能数量统计

### 验证方式
- 运行 `test_toolchain.sh coding/refactor` 确认工具链正常
- 测试 `bump-version.sh minor --dry-run` 和 `bump-version.sh current`
- 验证 CI YAML 语法

需要我开始实施吗？