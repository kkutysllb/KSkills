## Goal
Align the current KSkills project's stock-analysis layer and the `common/analysis-report` infrastructure skill with the reference finance skill package at `/Users/libing/kk_Projects/KWorks/skills/public/finance/`, and delete the obsolete `md-to-html-converter` skill.

The reference `analysis-report` (v2.3.0) defines a **strict three-file dual-theme contract**: one structured JSON payload → `{basename}.md` + `{basename}-dark.html` (bg `#101418`) + `{basename}-light.html` (bg `#ffffff`), enforced by a 549-line validator/renderer (`render_report.py`) with 5 pytest tests. It explicitly forbids buy/sell/hold advice and explicitly does NOT use any md-to-html tool.

---

## Part A — Delete `md-to-html-converter`

1. `git rm -r common/md-to-html-converter/` (7 tracked files: SKILL.md, CHANGELOG.md, install.sh, uninstall.sh, scripts/convert.py, scripts/package.sh, and the dir).
2. Delete the gitignored build artifact `dist/md-to-html-converter-1.0.0.skill` from disk.
3. Edit `README.md`:
   - Line ~17 tree comment: `common/     # 跨领域公共技能（3）` → `（2）`
   - Line ~67: `### 🧰 common — 跨领域公共（3）` → `（2）`
   - Line ~69: remove `` `md-to-html-converter`（Markdown 转 HTML）`` from the enumeration
   - Adjust the headline total-skill count (95 → 94) if present.
4. Leave historical `.qiongqisdd/plan/*.md` mentions untouched (frozen planning docs; not part of shipped skills).

Rationale: `analysis-report`'s reference SKILL.md explicitly states it does not use any md-to-html tool — deletion aligns the project with the reference design. No stock/common skill imports or references it.

---

## Part B — Upgrade `common/analysis-report` to v2.3.0 (mirror reference, adapted)

1. **Replace `common/analysis-report/SKILL.md`** with the reference v2.3.0 content, with two adaptations for THIS project:
   - Add the required `metadata.openclaw:` block (this project's `validate_skills.py` requires `metadata` for the rich profile; the reference SKILL.md omits it). Keep emoji/version/author/category/tags consistent.
   - Drop the reference's `assets:` frontmatter key (not in this project's `KNOWN_KEYS` → would WARN).
   - Substitute chart-visualization paths to the bare `chart-visualization/references/...` and `chart-visualization/scripts/generate.js` form already used by the current project (reference uses `skills/public/chart-visualization/...`).
   - Keep verbatim: the English description, the three-file deliverable rule, the execution chain, the input JSON contract, the chart-field discipline table, the no-buy/sell/hold rule, the self-check list, and the line *"本技能不调用任何 Markdown 转 HTML 工具，也不使用 md-to-html 类技能"* (which now lands cleanly after Part A).
2. **Add `common/analysis-report/scripts/render_report.py`** — copy the reference 549-line renderer/validator **verbatim** (stdlib-only: argparse/html/json/pathlib/typing; validates ≥3 charts, theme-variant correctness, dark/light data identity, per-tool field contracts; emits the 3 files).
3. **Add `common/analysis-report/tests/test_render_report.py`** — copy the reference 5 pytest tests verbatim (writes md+dark+light; rejects non-contract fields; rejects wrong theme variant; rejects unsupported style fields; rejects dark/light data mismatch).
4. **Rewrite `common/analysis-report/CHANGELOG.md`** to mirror the reference (entries 2.2.0, 2.1.0, 1.0.0) and add a `2.3.0` entry (current top is 1.0.0 — there's existing drift to fix).

No `install.sh` needed — knowledge-only skill, stdlib-only script, no pip deps.

---

## Part C — Wire analysis engines to delegate report generation to `analysis-report`

For each skill below, replace its ad-hoc report-output section with a **"报告生成（委托 analysis-report）"** subsection mandating: (1) build the analysis-report JSON payload; (2) for every chart read the matching `chart-visualization/references/generate_{type}.md`, generate **two** URLs (dark `theme:"dark"` + light `theme:"default"`, identical data); (3) invoke `analysis-report/scripts/render_report.py --input report.json --output-dir . --basename <date>_<topic>` to produce the 3 files; (4) list the 3 file paths in the reply. Also remove any mandatory "买入/卖出/持有/投资建议" language (per the reference's no-advice rule) — replace with research conclusions / scenarios / indicators-to-track / risk level.

**Tier 1 — full report-section rewrite (the 4 with existing `阶段三：报告输出`):**
- `stock/kk-stock-analysis/SKILL.md` (~line 328): add delegation subsection after the scoring-weight table.
- `stock/kk-futures-analysis/SKILL.md` (~lines 170-176): rewrite the 5-part list; replace "投资建议" (line 175) and "投资策略建议" (line 168) with scenario/indicator language.
- `stock/kk-industry-analysis/SKILL.md`: rewrite its 报告输出 section.
- `stock/a-stock-screener/SKILL.md`: rewrite its 报告输出 section; reword its self-declared `report-generation` capability description to note delegation to `analysis-report`.

**Tier 2 — add a delegation note (heavy engines that produce reports but lack an explicit section):**
- `stock/kk-etf-analysis/SKILL.md`
- `stock/kk-cb-analysis/SKILL.md`
- `stock/kk-market-linkage-engine/SKILL.md`

Out of scope this pass: `kk-options-payoff`, `kk-options-volatility`, `kk-factor-research`, `kk-strategy-research`, methodology skills (knowledge-only, no report mandate).

---

## Part D — Verification (run before declaring done)

1. `python3 scripts/validate_skills.py common/ stock/` → expect 0 ERRORs (covers all changed SKILL.md frontmatter).
2. `python3 -m pytest common/analysis-report/tests/ -v` → expect 5 passed.
3. Smoke-run the renderer end-to-end against the test fixture payload, confirm it emits `{basename}.md`, `{basename}-dark.html` (contains `#101418`, `data-theme="dark"`), `{basename}-light.html` (contains `#ffffff`, `data-theme="light"`), and that an invalid payload fails fast with no output files.
4. `python3 scripts/build_skill.py common/analysis-report` → confirm clean packaging with SKILL-MANIFEST.json injection.
5. Confirm `git status` shows the expected deletions, additions, and edits — no stray `__pycache__`/`.pytest_cache` staged (they're gitignored).

---

## Notes
- `md-to-html-converter`'s SKILL.md even references a stale absolute path (`/Users/libing/kk_Stock/kk_QuantFlows/...`) confirming it's orphaned legacy.
- The reference CHANGELOG tops out at 2.2.0 while its SKILL.md says 2.3.0 (minor source drift); I'll add a clean 2.3.0 entry.
- `__pycache__/`, `.pytest_cache/`, `*.egg-info/` are already gitignored, so running tests won't dirty the tree.